name: deploy
on:
    push:
        branches:
            - main
jobs:
    build: 
        runs-on: self-hosted
        steps: 
            - uses: actions/checkout@v2
            - name: PM2 Restart Server
              run: echo "halo"
            - name: PM2 Restart Sniffer
              run: echo "helo"
